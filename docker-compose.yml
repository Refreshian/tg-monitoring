services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: tg_monitoring
      POSTGRES_PASSWORD: tg_monitoring
      POSTGRES_DB: tg_monitoring
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
